package com.lukhol.hibernate.inheritance.tableperclass;
/**
 * Inheritance - TABLE_PER_CLASS
 * Require different GeneratedValue - GenerationType.TABLE.
 * All fields are in each table (no table for base properties like for JOINED).
 * Seems like also is using hibernate generator?
 */
